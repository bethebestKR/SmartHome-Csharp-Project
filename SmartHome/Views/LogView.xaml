<UserControl x:Class="SmartHome.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgPrimary}" FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <Border Grid.Row="0" BorderBrush="{StaticResource BorderSub}" BorderThickness="0,0,0,1" Padding="16,12,16,8">
            <Grid>
                <StackPanel>
                    <TextBlock Text="시스템 로그" FontSize="14" FontWeight="Medium" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="전체 이벤트 타임스탬프 기록" FontSize="11" Foreground="{StaticResource TextDisabled}" Margin="0,2,0,0"/>
                </StackPanel>
                <Button Content="로그 초기화" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Style="{StaticResource PrimaryButton}" Click="ClearLog_Click"/>
            </Grid>
        </Border>

        <!-- 필터 바 -->
        <Border Grid.Row="1" BorderBrush="{StaticResource BorderSub}" BorderThickness="0,0,0,1" Padding="16,8">
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Border Background="#1E3A5F" BorderBrush="{StaticResource BorderActive}" BorderThickness="1" CornerRadius="3" Padding="10,3" Cursor="Hand">
                    <TextBlock Text="전체" FontSize="10" Foreground="{StaticResource TextPrimary}"/></Border>
                <Border Background="#2A1010" BorderBrush="{StaticResource BorderDanger}" BorderThickness="1" CornerRadius="3" Padding="10,3" Cursor="Hand">
                    <TextBlock Text="경보" FontSize="10" Foreground="{StaticResource AccentRed}"/></Border>
                <Border Background="Transparent" BorderBrush="{StaticResource BorderSub}" BorderThickness="1" CornerRadius="3" Padding="10,3" Cursor="Hand">
                    <TextBlock Text="자동" FontSize="10" Foreground="{StaticResource TextDisabled}"/></Border>
                <Border Background="Transparent" BorderBrush="{StaticResource BorderSub}" BorderThickness="1" CornerRadius="3" Padding="10,3" Cursor="Hand">
                    <TextBlock Text="수동" FontSize="10" Foreground="{StaticResource TextDisabled}"/></Border>
                <Border Background="Transparent" BorderBrush="{StaticResource BorderSub}" BorderThickness="1" CornerRadius="3" Padding="10,3" Cursor="Hand">
                    <TextBlock Text="시스템" FontSize="10" Foreground="{StaticResource TextDisabled}"/></Border>
            </StackPanel>
        </Border>

        <!-- 로그 리스트 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="LogList" Margin="16,10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,3" CornerRadius="5" Padding="12,7"
                                BorderThickness="3,0,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{StaticResource BgCard}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderCard}"/>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="85"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Time}"
                                           FontSize="10" FontFamily="Consolas"
                                           Foreground="{StaticResource TextDisabled}"
                                           VerticalAlignment="Center"/>
                                <Border Grid.Column="1" CornerRadius="3" Padding="6,1"
                                        Background="{StaticResource BgDeep}"
                                        BorderBrush="{StaticResource BorderCard}" BorderThickness="1"
                                        VerticalAlignment="Center" Margin="0,0,8,0">
                                    <TextBlock Text="{Binding Room}" FontSize="10"
                                               Foreground="{StaticResource TextMuted}"
                                               HorizontalAlignment="Center"/>
                                </Border>
                                <TextBlock Grid.Column="2" Text="{Binding Message}"
                                           FontSize="11" Foreground="{StaticResource TextSecondary}"
                                           VerticalAlignment="Center" TextWrapping="Wrap"/>
                                <Border Grid.Column="3" CornerRadius="3" Padding="6,1"
                                        Background="{StaticResource BgDeep}"
                                        BorderBrush="{StaticResource BorderCard}" BorderThickness="1"
                                        VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding Type}" FontSize="9"
                                               Foreground="{StaticResource TextMuted}"
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
